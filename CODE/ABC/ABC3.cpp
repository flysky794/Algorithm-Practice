#include <iostream>
#include <vector>
using namespace std;

int main()
{
    long long N = 0, Q = 0;
    cin >> N >> Q;
    vector<long long> nums(N + 1, 0);
    for(long long i = 1; i <= N; i++)
        cin >> nums[i];
    while(Q--)
    {
        long long opera = 0;
        cin >> 
    }
    return 0;
}